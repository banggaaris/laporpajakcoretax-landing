---
import { Clock, Lock, History, Wallet, MessageCircle } from 'lucide-react';
---

<section id="features" class="py-24 bg-white overflow-hidden">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <h2 class="text-2xl md:text-3xl font-extrabold text-gray-900 mb-4">Mengapa Memilih Kami?</h2>
      <p class="text-gray-500">Layanan terbaik dengan teknologi dan keamanan terdepan.</p>
    </div>

    <!-- Bento Grid Layout -->
    <div class="grid grid-cols-1 md:grid-cols-3 md:grid-rows-2 gap-4 auto-rows-[240px]">
      
      <!-- Card 1: Proses Ekspres (Large) -->
      <div class="md:col-span-2 md:row-span-1 bg-orange-50 rounded-3xl p-8 border border-orange-100 relative overflow-hidden group">
        <div class="relative z-10 flex flex-col h-full justify-between">
          <div>
            <h3 class="text-xl font-bold text-gray-900 mb-2">Proses Ekspres</h3>
            <p class="text-gray-600 max-w-sm">Selesai dalam 2x24 jam. Tim kami bekerja secara paralel untuk kecepatan maksimal.</p>
          </div>
          <div class="w-full bg-white h-4 rounded-full overflow-hidden border border-indigo-200 mt-6 relative">
             <div class="bg-[#FF6B00] h-full rounded-full animate-fill shadow-[0_0_15px_rgba(255,107,0,0.5)]"></div>
          </div>
        </div>
        <Clock className="absolute -right-4 -bottom-4 w-32 h-32 text-orange-200 opacity-20 transform -rotate-12 group-hover:rotate-0 transition-transform duration-700" />
      </div>

      <!-- Card 2: Privasi (Small) -->
      <div class="bg-gray-900 rounded-3xl p-8 text-white relative overflow-hidden flex flex-col justify-between group">
        <div class="relative z-10">
          <Lock className="w-10 h-10 text-[#FF6B00] mb-4" />
          <h3 class="text-lg font-bold mb-2">Privasi Terjamin</h3>
          <p class="text-gray-400 text-sm">Kerahasiaan data adalah prioritas utama kami.</p>
        </div>
        <div class="absolute inset-0 bg-gradient-to-t from-[#FF6B00]/10 to-transparent pointer-events-none"></div>
        <div class="animate-scan"></div>
      </div>

      <!-- Card 3: Pengalaman Sejak 2020 (Small) -->
      <div class="bg-blue-50 rounded-3xl p-8 border border-blue-100 relative overflow-hidden flex flex-col justify-center group">
        <div class="relative z-10">
          <History className="w-10 h-10 text-blue-600 mb-4" />
          <h3 class="text-lg font-bold text-gray-900 mb-2">Sejak 2020</h3>
          <p class="text-gray-600 text-sm">Berpengalaman membantu ribuan pelaporan pajak pribadi setiap tahunnya.</p>
        </div>
      </div>

      <!-- Card 4: Harga (Small) -->
      <div class="bg-white rounded-3xl p-8 border border-gray-100 shadow-sm flex flex-col items-center justify-center text-center group hover:shadow-lg transition-all">
         <div class="w-20 h-20 bg-orange-100 rounded-full flex items-center justify-center mb-4 animate-float-slow">
            <Wallet className="w-10 h-10 text-[#FF6B00]" />
         </div>
         <h3 class="text-lg font-bold mb-2">Harga Secangkir Kopi</h3>
         <p class="text-gray-500 text-sm italic">Cuma Rp50.000, hemat waktu berjam-jam.</p>
      </div>

      <!-- Card 5: Bantuan 24/7 (Small) -->
      <div class="bg-gray-50 rounded-3xl p-8 border border-gray-100 relative overflow-hidden flex flex-col justify-center group">
        <div class="relative z-10">
          <div class="w-12 h-12 rounded-full bg-green-100 flex items-center justify-center mb-4">
              <MessageCircle className="w-6 h-6 text-green-600 animate-pulse" />
          </div>
          <h3 class="text-lg font-bold text-gray-900 mb-2">Bantuan 24/7</h3>
          <p class="text-gray-600 text-sm">Konsultasi langsung via WhatsApp kapan saja terkait Coretax.</p>
        </div>
      </div>

    </div>
  </div>
</section>
